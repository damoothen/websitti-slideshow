(function($){

    $.fn.slideshow = function(params){

        var defaults = {

        };

        params = $.extend({}, defaults, params);

        return this.each(function(){

            var $slider = $(this);

        });

    };

})(jQuery);
